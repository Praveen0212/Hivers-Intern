# Optional: API keys for LLM Judge or external generative model (not required for standard evaluation)
OPENAI_API_KEY=
GEMINI_API_KEY=

# Optional: Custom data sampling and threshold configurations
DATA_SAMPLE_MB=40
CLASSIFIER_CONFIDENCE_THRESHOLD=0.35
RETRIEVAL_SIMILARITY_THRESHOLD=0.15
